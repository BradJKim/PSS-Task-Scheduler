package com.models;

public class TransientTask extends Task {
    // Constructor
    public TransientTask(String id, String name, int date, String startTime, double duration, String type) {
        super(id, name, date, startTime, duration, type);
    }
}